class EarthType {
    private type: string;
    private height: number;
    private width: number;

    constructor(width, height, type) {
        this.width = width;
        this.height = height;
        this.type = type;
    }
}
